// import remToPx from 'tailwindcss-preset-px-to-rem';

export default {
  // presets: [remToPx],
  // theme: {
  //   remDivider: 16, // px -> em 변환 기준 값 (기준값 20px)
  //   fontSizeLimit: 100, // 글꼴 크기 변환의 최대 px 값 (기준값 100px)
  //   spacingLimit: 1920, // 간격 변환의 최대 px 값 (기준값 1920px)
  //   borderRadiusLimit: 500, // 테두리 반경 변환의 최대 px 값 (기준값 500px)
  // },
};
